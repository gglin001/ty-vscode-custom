[project]
name = "ty-vscode"
version = "2026.4.0"
requires-python = ">=3.8"
dependencies = ["ty==0.0.12"]

[tool.ruff]
line-length = 88
target-version = "py37"

[tool.ruff.lint]
select = ["E", "F", "W", "Q", "UP", "I", "N"]
